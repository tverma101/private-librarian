# Codex turn log

## 2026-08-30 — local update, packaging, and model setup

- `scope`: canonical `/Users/tejas/Projects/private-librarian`; update the local branch, finish the macOS distributable, and make optional Python model setup repeatable and offline at runtime.
- `project`: `git@github.com:tverma101/private-librarian.git`, branch `fix/tier2-incremental-ci`, fast-forwarded to `169ad9140c978a6093f00277dca29151b832d285` (`origin/fix/tier2-incremental-ci`).
- `status`: implementation complete for this requested boundary; changes remain uncommitted and were not pushed.
- `changed_files`: `VERSION`, `.gitignore`, `README.md`, `docs/SECURITY.md`, `docs/VERIFICATION.md`, `docs/codex/turn-log.md`, `script/build_and_run.sh`, `scripts/embed.py`, `scripts/model-requirements.txt`, `scripts/package_app.sh`, `scripts/provision_image_models.py`, `scripts/setup_models.sh`, `scripts/test_model_provisioning.py`, `Sources/LibrarianCore/LocalModelBridge.swift`, `Sources/LibrarianCore/Indexer.swift`, `Sources/LibrarianApp/PrivateLibrarianApp.swift`, and `Sources/LibrarianApp/MagicViews.swift`.
- `model_setup`: the isolated Python runtime is installed at the app-container `~/Library/Containers/com.tejas.private-librarian/Data/Library/Application Support/PrivateLibrarian/model-runtime`; pinned CLIP ViT-B/32 and all-MiniLM-L6-v2 are installed at the matching `Models` root with schema-2 SHA-256 provenance manifests. The legacy ignored checkout cache was moved to `/tmp/private-librarian-legacy-models.ol1ZQu/Models` for recovery.
- `packaging`: the canonical release output is `dist/PrivateLibrarian-0.1.0.dmg`; the temporary app is staged under ignored `.build/` output, includes SwiftPM resources and the helper, excludes `librarian-cli`, and is removed after DMG/install. The installed copy is `/Applications/PrivateLibrarian.app`. Older generated bundles were moved to `/tmp/private-librarian-legacy-dist.qPXL7D` for recovery.
- `validation`: `swift test` passed 153 tests with 0 failures; Swift 6 warnings-as-errors build passed; release build passed; `scripts/e2e_local.sh` passed all ten checks; model manifest fixture tests passed; both model manifests passed offline verification; `embed.py --check` reported both models/dependencies ready; real offline inference produced 512-D CLIP image/text and 384-D MiniLM vectors; the release CLI Tier-2 smoke indexed 13 fixtures with `clipCalls=2`, `textEmbedCalls=4`, and 4 persisted embeddings; setup rerun was idempotent; entitlement audit, deep code-sign verification, DMG creation/verification, and installed-app launch all passed.
- `evidence_state`: implemented=yes; tested=yes; model runtime and weights installed=yes; packaged and installed=yes; live launch=yes; user-confirmed folder/bookmark relaunch smoke=no.
- `residual_gap`: issue #44 still needs the human App Sandbox flow (select a real folder, quit/relaunch, confirm bookmark restoration and FSEvent reindexing). Core ML MobileCLIP artifacts remain optional/unprovisioned; the verified local Python path is ready. GitHub Actions were not run or changed.
- `cleanup`: pre-existing dirty backlog work was preserved in recoverable `stash@{0}` (`codex preserve pre-update private-librarian backlog`); unrelated older stashes were left untouched. No permanent deletion, push, merge, or workflow mutation was performed.
- `memory_decision`: existing memory was consulted for canonical checkout/release boundaries; no memory update was made because the user did not explicitly request one.
- `rollout_refs`: current Codex turn, 2026-08-30.

## 2026-08-30 — remove duplicate app staging from the release path

- `scope`: resolve the user-visible duplicate app and launch failure caused by leaving both the generated `dist/PrivateLibrarian.app` and installed app in place.
- `project`: canonical `/Users/tejas/Projects/private-librarian`, `fix/tier2-incremental-ci`; changes remain uncommitted and unpushed.
- `changed_files`: `scripts/package_app.sh`, `script/build_and_run.sh`, `README.md`, `docs/VERIFICATION.md`, `docs/SECURITY.md`, `docs/troubleshooting/keychain-prompts.md`, and this turn log.
- `packaging_fix`: normal packaging now keeps only the versioned DMG in `dist/`; the app is staged in ignored `.build/package-stage/`, installed only when requested, and removed after DMG/install. The Run button installs and opens only `/Applications/PrivateLibrarian.app`. Exact old generated app names in `dist/` are moved to a recoverable `.build/private-librarian-legacy-dist.*` directory.
- `validation`: before this change, the filesystem inventory showed exactly two matching bundle IDs (`dist/PrivateLibrarian.app` and `/Applications/PrivateLibrarian.app`) and no other Private Librarian install; the final archive/package verification is pending after this staging change.
- `evidence_state`: diagnosis=yes; implementation=yes; duplicate cleanup/package rerun pending; user-confirmed launch=no because the legacy Keychain approval is still human-controlled.
- `residual_gap`: after the next package run, confirm Spotlight lists only the installed bundle and complete one-time Keychain approval, then relaunch. Public Gatekeeper acceptance still needs Developer ID + notarization.
- `cleanup`: no user data or Keychain item is deleted; generated app copies are moved recoverably; no GitHub or Actions mutation.

## 2026-08-30 — stable Xcode signing and legacy Keychain migration

- `scope`: stop repeated Apple Keychain prompts while preserving the existing encrypted catalog, complete the Xcode-backed package, and keep model/CLI setup prompt-free.
- `project`: canonical `/Users/tejas/Projects/private-librarian`, `fix/tier2-incremental-ci`, remote `git@github.com:tverma101/private-librarian.git`; HEAD remains aligned with `origin/fix/tier2-incremental-ci` and changes remain uncommitted/unpushed.
- `root_cause`: the matching login-keychain item was inspected and its ACL names the removed `/Users/tejas/Code/private-librarian/.build/arm64-apple-macosx/debug/librarian-cli` plus a stale code hash. Earlier ad-hoc rebuilds therefore appeared as new Keychain clients.
- `changed_files`: `Sources/LibrarianCore/CatalogKeychain.swift`, `Sources/LibrarianCore/Catalog.swift`, `Sources/LibrarianApp/PrivateLibrarianApp.swift`, `Sources/LibrarianApp/MagicViews.swift`, `Sources/librarian-cli/main.swift`, `scripts/package_app.sh`, `README.md`, `docs/ARCHITECTURE.md`, `docs/SECURITY.md`, `docs/VERIFICATION.md`, `docs/troubleshooting/keychain-prompts.md`, and this turn log.
- `keychain_fix`: the GUI now queries the signed app-owned data-protection item first; if the item is absent it reads the legacy item once, preserves the same 32-byte key, writes the app-owned item, and caches one result/failure per process. The final package carries the certificate team identifier, application identifier, and team-scoped Keychain group. The CLI requires `LIBRARIAN_CATALOG_KEY` and never opens the GUI item.
- `catalog_recovery`: the legacy `catalog.db`, `catalog.db-shm`, and `catalog.db-wal` were copied without modification into the sandbox Application Support directory; the original files remain in place.
- `validation`: the latest run passed 159 Swift tests, strict-concurrency warnings-as-errors build, release build, ten-check E2E against the rebuilt CLI, model fixture/offline checks, Xcode generic archive/package/install, universal executable + dSYM, stable Apple Development signature, sandbox/hardened entitlement audit, deep verification, DMG verification, no duplicate app/CLI payload, and matching installed executable hashes. The signed entitlements were inspected and include the expected app identifier/group.
- `evidence_state`: implementation=yes; static/build/package verification=yes; installed artifact=yes; launch=yes; prompt-free post-migration behavior and the real App Sandbox folder/bookmark lifecycle remain user-confirmed=no because the one legacy approval cannot be granted programmatically in this run.
- `residual_gap`: launch the installed app and choose **Always Allow** once for the legacy migration, then quit/relaunch and confirm no prompt. Public distribution still needs Developer ID Application signing, notarization, and stapling. Computer Use was unavailable, and the 24 GB benchmark was not completed.
- `cleanup`: no Keychain item or catalog was deleted; generated legacy bundles/models remain recoverable in `/tmp`; no push, merge, GitHub Actions run, issue mutation, or permanent deletion.
- `memory_decision`: existing canonical-checkout and Private Librarian release memory was consulted; no memory update was made because the user did not explicitly request one.
- `rollout_refs`: current Codex turn, 2026-08-30.

## 2026-08-30 — make startup launchable and remove restricted entitlement failure

- `scope`: resolve the remaining inability to open the installed app after the duplicate-app packaging fix, stop automatic legacy Keychain prompts, and leave one usable distributable.
- `project`: canonical `/Users/tejas/Projects/private-librarian`, `fix/tier2-incremental-ci`; changes remain uncommitted and unpushed.
- `root_cause`: AMFI rejected the prior bundle at spawn time because the packager manually injected restricted `application-identifier`, team, and Keychain-group entitlements without an embedded provisioning profile. `codesign --verify` passed, but launch logs reported `No matching profile found`; Launch Services returned RBS code 5 / POSIX error 163 and direct execution exited 137.
- `changed_files`: `Sources/LibrarianCore/CatalogKeychain.swift`, `Sources/LibrarianApp/PrivateLibrarianApp.swift`, `Sources/LibrarianApp/MagicViews.swift`, `scripts/package_app.sh`, `scripts/embed.py`, `README.md`, `docs/SECURITY.md`, `docs/VERIFICATION.md`, `docs/troubleshooting/keychain-prompts.md`, `docs/troubleshooting/packaging-launch.md`, and this turn log.
- `keychain_startup_fix`: the GUI no longer performs legacy login-Keychain lookup in model initialization. New catalogs use `createAppOwned()`, existing catalogs render a visible **Migrate Existing Catalog** action, and that action is the only legacy lookup and is disabled after one attempt. The encrypted catalog key is never rotated or deleted.
- `packaging_fix`: the packager now signs only the profile-free sandbox/read-only entitlement template; it does not add restricted Keychain-sharing entitlements without a provisioning profile. It stages under ignored `.build/`, leaves only `dist/PrivateLibrarian-0.1.0.dmg`, installs only `/Applications/PrivateLibrarian.app`, and removes the temporary staged bundle. `script/build_and_run.sh` uses that canonical installed path.
- `validation`: `swift test --disable-sandbox` passed 159 tests with 0 failures; `scripts/package_app.sh --xcode --install` archived successfully; the installed app passed deep signing and entitlement audits, is universal `arm64` + `x86_64`, and the DMG passed `hdiutil verify`. `open -n /Applications/PrivateLibrarian.app` returned 0 and `LibrarianApp` remained alive. `dist/` contains only the DMG, `/Applications` contains one matching app, and Spotlight returns only that app.
- `evidence_state`: implementation=yes; tested=yes; packaged/installed=yes; live launch=yes; prompt-free legacy migration and real App Sandbox folder/bookmark relaunch remain user-confirmed=no.
- `residual_gap`: public Gatekeeper distribution still needs Developer ID Application signing, notarization, and stapling. The current legacy catalog requires the user to press **Migrate Existing Catalog** and choose **Always Allow** once; no automated approval was attempted.
- `cleanup`: stale generated bundles are recoverable under ignored `.build/private-librarian-legacy-dist.*`; no catalog, Keychain item, source file, or model data was deleted; no push, merge, Actions run, or issue mutation occurred.
- `memory_decision`: existing canonical-checkout and Private Librarian release memory was consulted; no memory update was made because the user did not explicitly request one.
- `rollout_refs`: current Codex turn, 2026-08-30.
